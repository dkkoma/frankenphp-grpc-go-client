# Initial Extension Repository Design

## State

closed

## Source

user

## Context

Create this repository's `AGENTS.md` by referencing `~/src/php-grpc-lite-gax`, then design the artifacts for a FrankenPHP PHP extension written in Go and backed by grpc-go. The consumer-side API expectation is `~/src/php-grpc-lite-gax/docs/frankenphp-extension-api.md`.

## Acceptance Criteria

- `AGENTS.md` exists and reflects this repository's Go/FrankenPHP extension responsibilities.
- `docs/design.md` describes the public PHP surface, internal layout, runtime model, grpc-go mapping, lifecycle rules, test strategy, milestones, and risks.
- Issue tracking scaffolding exists for future work.

## Verification

Verified by reading `AGENTS.md` and `docs/design.md` after creation.
